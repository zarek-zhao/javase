package hanshunping.list_;

/**
 * @author zarek
 * @version 1.0.0
 * @since 1.0.0
 */
public class ListMethod {
    public static void main(String[] args)
    {
        /*
        List接口下的常用方法
        add(index,element)
        addAll(list)
        get(index)
        indexOf(element)
        lastIndexOf(element)
        remove(index)
        set(index,element)
        subList(fromIndex,toIndex)返回从fromIndex到toIndex位置的自己和 前闭后开
         */
    }
}
